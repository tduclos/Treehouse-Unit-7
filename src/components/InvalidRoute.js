  import React from 'react';

// Lets the user know they've tried to access an invalid route

const InvalidRoute = () => {
  return (
    <ul>
        <h3>Nice try, but that route does not exist.</h3>
    </ul>
  );
};

export default InvalidRoute;